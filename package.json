{
  "name": "blade-account-card",
  "version": "1.0.0",
  "private": true,
  "description": "File-based cryptographic identity and credential system for Cloudflare Pages + D1",
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}