import{json,body,rid,now,admin,log,schema,sha}from"../_lib.js";
const urls=x=>{try{return JSON.parse(x)}catch{return[]}};
export async function onRequestGet(c){const a=await admin(c);if(a.error)return a.error;await schema(c.env.DB);const r=await c.env.DB.prepare("SELECT id,name,client_id,redirect_uris,status,created_at FROM applications ORDER BY created_at DESC").all();return json({applications:r.results.map(x=>({...x,redirect_uris:urls(x.redirect_uris)}))})}
export async function onRequestPost(c){const a=await admin(c);if(a.error)return a.error;await schema(c.env.DB);const x=await body(c.request),name=String(x.name||"").trim(),u=String(x.redirect_uri||"").trim();if(!name||!u)return json({error:"Name and redirect URI are required"},400);let parsed;try{parsed=new URL(u);if(!["https:","http:"].includes(parsed.protocol))throw 0}catch{return json({error:"Invalid redirect URI"},400)};const id=rid("app"),client_id=rid("bac_app"),secret=rid("bac_secret")+rid("");await c.env.DB.prepare("INSERT INTO applications(id,name,client_id,client_secret_hash,redirect_uris,status,created_at) VALUES(?,?,?,?,?,'active',?)").bind(id,name,client_id,await sha(secret),JSON.stringify([u]),now()).run();await log(c.env.DB,a.person.id,"application.create",id,{client_id});return json({ok:true,id,client_id,client_secret:secret,redirect_uris:[u],warning:"Save the client secret now. BAC stores only its hash."},201)}
export async function onRequestPatch(c){const a=await admin(c);if(a.error)return a.error;const x=await body(c.request);if(!x.id)return json({error:"Application id required"},400);if(x.status&&!["active","disabled"].includes(x.status))return json({error:"Invalid status"},400);await c.env.DB.prepare("UPDATE applications SET status=COALESCE(?,status) WHERE id=?").bind(x.status||null,x.id).run();await log(c.env.DB,a.person.id,"application.update",x.id,{status:x.status});return json({ok:true})}
