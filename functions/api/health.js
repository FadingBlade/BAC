import{json,ensureSchema}from"../_lib.js";export async function onRequestGet(c){if(!c.env.DB)return json({ok:false,checks:{functions:true,d1:false},message:"Bind a D1 database as DB."},503);try{await ensureSchema(c.env.DB);const r=await c.env.DB.prepare("SELECT 1 AS n").first();return json({ok:r?.n===1,checks:{functions:true,d1:r?.n===1,schema:true},version:"1.1.0"})}catch(e){return json({ok:false,checks:{functions:true,d1:true,schema:false},message:String(e?.message||e)},500)}}
