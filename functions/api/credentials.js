import{json,body,rid,now,admin,log}from"../_lib.js";export async function onRequestGet(c){const a=await admin(c);if(a.error)return a.error;return json({credentials:(await c.env.DB.prepare(`SELECT cr.*,p.display_name,p.username FROM credentials cr JOIN people p ON p.id=cr.person_id ORDER BY cr.created_at DESC`).all()).results})}export async function onRequestPost(c){const a=await admin(c);if(a.error)return a.error;const x=await body(c.request);let k;try{k=x.public_key_jwk;if(k.kty!=="EC"||k.crv!=="P-256"||!k.x||!k.y)throw 0}catch{return json({error:"Invalid ECDSA P-256 public key"},400)}const id=rid("cred"),exp=new Date(Date.now()+Math.min(Math.max(Number(x.days)||365,1),3650)*86400000).toISOString();await c.env.DB.prepare("INSERT INTO credentials(id,person_id,label,public_key_jwk,algorithm,status,created_at,expires_at,file_version) VALUES(?,?,?,?,?,?,?,?,2)").bind(id,x.person_id,String(x.label||"BAC File Credential"),JSON.stringify(k),"ECDSA-P256","active",now(),exp).run();await log(c.env.DB,a.person.id,"credential.issue",id,{person_id:x.person_id});return json({ok:true,id,expires_at:exp,issued_at:now()},201)}