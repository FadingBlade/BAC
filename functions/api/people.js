import{json,body,rid,now,admin,log}from"../_lib.js";export async function onRequestGet(c){const a=await admin(c);if(a.error)return a.error;return json({people:(await c.env.DB.prepare("SELECT * FROM people ORDER BY created_at DESC").all()).results})}export async function onRequestPost(c){const a=await admin(c);if(a.error)return a.error;const x=await body(c.request),n=String(x.name||"").trim(),u=String(x.username||"").trim().toLowerCase().replace(/[^a-z0-9._-]/g,"");if(!n||u.length<2)return json({error:"Name and username required"},400);const id=rid("bac");try{await c.env.DB.prepare("INSERT INTO people VALUES(?,?,?,?,?,?)").bind(id,n,u,x.role==="admin"?"admin":"user","active",now()).run()}catch{return json({error:"Username already exists"},409)}await log(c.env.DB,a.person.id,"person.create",id);return json({ok:true,id},201)}export async function onRequestPatch(c){const a=await admin(c);if(a.error)return a.error;const x=await body(c.request);if(x.id===a.person.id&&x.status==="disabled")return json({error:"Cannot disable your current account"},400);await c.env.DB.prepare("UPDATE people SET role=?,status=? WHERE id=?").bind(x.role==="admin"?"admin":"user",x.status==="disabled"?"disabled":"active",x.id).run();await log(c.env.DB,a.person.id,"person.update",x.id);return json({ok:true})}